# Project Collaboration Rules

这是一个只供本人使用的互动式学习电纸书项目，也是用户学习 Web 开发和 AI 协作开发的真实项目。

## 开始工作前

- 先读取 `docs/PRD.md`、`docs/ARCHITECTURE.md`、`docs/DATA_MODEL_AND_API.md`、`docs/UI_SPEC.md` 和 `docs/STATUS.md`。
- 如果这些文档为空或内容不足，先说明缺口，不要擅自补充产品目标。
- 不修改工作区之外的记忆文件，除非用户明确要求。

## 讲解和实现方式

- 从一个用户能看到的具体动作开始。
- 按“用户动作 → 前端函数 → 请求 → 后端处理 → 数据保存 → 页面结果”的顺序解释。
- 每次只追踪一条短功能链，说明涉及的文件、函数和数据。
- 少放完整代码；代码出现时，先说明它在当前功能中的作用。
- 修改代码后说明验证方法、可能的失败位置和下一步。

## 范围控制

- 这是个人项目，优先完成一个可使用的最小闭环。
- 不擅自加入多用户、支付、复杂部署或与当前功能无关的基础设施。
- 技术方案未确认前，使用“暂定方案”或“待讨论”标记，不把候选方案当成最终决定。
